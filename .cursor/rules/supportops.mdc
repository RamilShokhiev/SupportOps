---
description: SupportOps Codex map — spec vs existing MVP, do not restart
alwaysApply: true
---

# SupportOps

Read `AGENTS.md`, `SupportOps_AI_project_plan.md`, and `docs/PROGRESS.md` first.

- This repo **is** the MVP. Do not create `supportops-ai`. Keep existing changes. Read current PROGRESS: packaging, migrations, scenario tests and evaluation artifacts are implemented; continue only from remaining gaps.
- Model has no tools. Graph ends at human interrupt. Execute only an approved `content_hash`. `next_step` is server-side. Conflict groups → clarify.
- Decisions in short Russian; code and README in English. Demo results are not live LLM/GitHub results.
- `config.py` now resolves `PROJECT_ROOT` inside the repo. Compose runs migrations/bootstrap before the API and worker.
